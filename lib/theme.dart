import 'package:moo/design.dart';

ThemeData makeTheme() {
  return ThemeData(
    brightness: .dark,
    colorSchemeSeed: Palette.neon,
    scaffoldBackgroundColor: Palette.radiation,
    fontFamily: FontFamily.shareTechMono,
  );
}
