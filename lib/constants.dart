/// The size of the terminal, in pixels.
const TERMINAL_SIZE = 880.0;

/// The size of a single cell in the file system grid, in pixels.
const CELL_SIZE = 64.0;

/// Maximum size for the file system grid, in cells.
const MAX_FS_SIZE = 10;

/// Maximum errors allowed before failure during gameplay.
const MAX_ERRORS = 3;

/// Points deducted for retrying a level.
const REDO_COST = 5000;
